<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="XUITheme.Phone">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MemberDetailItem">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">60dp</item>
        <item name="paddingStart">5dp</item>
        <item name="paddingEnd">5dp</item>
        <item name="sLeftTextSize">20dp</item>
        <item name="sLeftViewMarginLeft">25dp</item>
        <item name="sRightIconRes">@drawable/ic_arrow_right</item>
    </style>

    <style name="PersonalInformationItem">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">60dp</item>
        <item name="android:paddingStart">5dp</item>
        <item name="android:paddingEnd">5dp</item>
        <item name="sLeftIconMarginLeft">15dp</item>
        <item name="sLeftTextSize">16dp</item>
        <item name="sLeftViewMarginLeft">20dp</item>
        <item name="sRightIconRes">@drawable/ic_arrow_right</item>
    </style>

</resources>
